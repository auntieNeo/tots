#include "subsystem.h"

namespace tots {
  Subsystem::Subsystem() {
  }

  Subsystem::~Subsystem() {
  }

  void Subsystem::update(const GameState *state) {
    // TODO: determine and update the delta time
  }
}
