#include "hoggedThread.h"

namespace tots {
  HoggedThread::HoggedThread(const char *name, const GameState *gameState) {
  }

  HoggedThread::~HoggedThread() {
  }

  void HoggedThread::run(Subsystem *subsystem, SubsystemThread::Command command) {
  }
}
