#include "messageQueue.h"

namespace tots { namespace framework {
  /**
   *
   */
  /*
  void MessageQueue::sendMessage(const Message &message) {
    self->append(message);
  }
  */

  /*
  void MessageQueue::merge(const MessageQueue &queue) {
  }
  */
} }
